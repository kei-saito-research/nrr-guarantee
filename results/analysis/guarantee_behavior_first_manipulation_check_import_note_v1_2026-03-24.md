# Guarantee behavior-first manipulation-check import note v1

Date: 2026-03-24
Status: local package note
Purpose:
- record the bounded manuscript role of the repaired behavior-first provider-pair rerun
  inside the current `nrr-guarantee` package
- keep the new `v26` manuscript prose auditable without silently promoting the
  rerun into a fourth filled claim unit

## 1. What This Note Covers

The current manuscript `v26` adds one bounded behavior-first paragraph in
`Results` plus one paired limitation echo in `Discussion`.

These insertions do not create a new filled `Guarantee` claim unit.
Their manuscript role is narrower:

- a manipulation-check result
- bounded behavioral-separation evidence

## 2. What The Current Package Uses

The current package uses the behavior-first rerun only to support this narrow
read:

- repaired opening-comparison insertion changes the framing of the next move on
  the tested provider pair
- implementation responses mostly remain in direct relation work
- ablation responses mostly open with comparison framing before continuing into
  a follow-up question

## 3. What The Current Package Does Not Use It For

The current package does not use the behavior-first rerun as evidence for:

- a clean two-stage success
- pair-level continuation-quality superiority
- a fourth supported `Guarantee` claim unit
- a broader `problem solved` conclusion

## 4. Mandatory Attached Limitation

Where the current manuscript mentions the behavior-first rerun, the following
limitations remain attached:

- Anthropic `D2` remains a `1 / 10` collapse row
- ablation realization is still compressed rather than cleanly separated
- the result is bounded behavioral-separation evidence only

## 5. Practical Consequence

The current repository package should therefore be read as:

- three filled bounded claim units
- one later-horizon failure boundary
- one additional behavior-first manipulation-check note carried inside
  manuscript prose only

The package remains bounded and truthful only if that behavior-first material is
kept in this narrower supporting role.
