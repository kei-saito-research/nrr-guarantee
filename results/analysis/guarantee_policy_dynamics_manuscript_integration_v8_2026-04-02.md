# Guarantee Policy/Dynamics Manuscript Integration v8

## Status
- Date: 2026-04-02
- Type: guarantee drafting memo
- Scope: resync the current manuscript-facing policy/dynamics guidance to the
  `v48` one-supported-claim-plus-boundaries ontology and the current package
  audit surface
- Depends on:
  - `supporting_layer_sources/current/policy_minimal_spec_current_v1_2026-03-18.md`
  - `supporting_layer_sources/current/energy_dynamics_mechanism_skeleton_candidate_v2_2026-03-17.md`
  - `supporting_layer_sources/current/non_evaluative_principle_dynamics_closure_note_v1_2026-03-19.md`
  - `guarantee_first_claim_unit_fill_v3_2026-03-30.md`
  - `guarantee_downstream_acceptance_boundary_surface_v2_2026-03-31.md`
  - `guarantee_side_topic_supporting_surface_v1_2026-03-30.md`
  - `guarantee_later_horizon_failure_boundary_v3_2026-03-30.md`
  - `guarantee_behavior_first_manipulation_check_import_note_v8_2026-04-02.md`
- Supersedes:
  - `guarantee_policy_dynamics_manuscript_integration_v7_2026-03-31.md`

## 1. Assistant-Side Decision
- judgment:
  - `adopt`
- reason:
  - the package's current guidance surface must match the repaired `v48`
    ontology exactly, otherwise downstream notes reopen the same authority
    ambiguity that the confirm review flagged

## 2. Policy Vocabulary To Surface Mapping
The current manuscript should make the following links explicit:

1. early ambiguity-turn supported claim
- main policy action:
  - `defer_with_resolution`
- protected boundary:
  - no unilateral direction choice before `accepted_direction`

2. downstream-acceptance boundary surface
- main policy action:
  - `commit_to_accepted_direction`
- protected boundary:
  - keep the carried support limited to
    `preacceptance_rework_burden_absent`
  - keep the Gemini-side mainline regression explicit:
    `accepted_direction_followthrough_present` drops from `13 / 24` to
    `10 / 24`
  - do not let the auxiliary repaired `NRR vs Ablation` read replace the
    mainline `baseline vs Energy policy` contract
  - do not widen the line into pair-wide downstream-quality closure

3. first-step side-topic supporting surface
- main policy state read:
  - accepted side direction becomes `active_objective`
  - prior main line becomes `suspended_objective`
  - `quarantine` must remain intact
- protected boundary:
  - keep the suspended main line from re-entering first-step workstart
  - keep this line as supporting evidence only because it sits on the repaired
    local `baseline vs revised Energy policy` comparison

4. later-horizon failure boundary
- main policy stress point:
  - `unresolved_relation`
  - `local_relation_block`
- protected boundary:
  - later turns should not silently collapse unresolved meaning
  - keep later-horizon as a separate `priority-resolution horizon-stability`
    family that reads the later continuation of the carried side-topic line
    rather than as the same repaired first-step family
  - keep the current field-fixed observability scope explicit on
    `chosen_side_direction_progress_present`,
    `user_detail_use_present`,
    `unresolved_relation_preserved_or_narrowly_checked`,
    `suspended_main_reference_absent`,
    `starter_question_repeat_absent`, and `appropriate`
  - keep the comparison scope explicitly diagnostic one-arm rather than matched

5. behavior-first manipulation check
- manuscript role:
  - secondary manipulation-check note only
- protected boundary:
  - do not promote it into policy-sufficiency, downstream-boundary, or
    continuation-quality support

## 3. Dynamics Vocabulary To Surface Mapping
The manuscript should tie each current surface to the current mechanism motifs:

1. early ambiguity-turn supported claim
- `Stable Resolution`

2. downstream-acceptance boundary surface
- removal of preacceptance burden under accepted-direction commitment
- current motif read:
  - useful handoff support with provider-sensitive residuals still visible
  - the mainline Gemini slice includes a visible followthrough regression
    (`13 / 24 -> 10 / 24`)
- protected boundary:
  - not pair-wide downstream-quality closure

3. first-step side-topic supporting surface
- `Clean Quarantine Hold`
- protected boundary:
  - first-step only
  - not later-horizon quarantine stability

4. later-horizon failure boundary
- `Relation-Preservation Hold`
- `Premature Relation Collapse`
- secondarily, where relevant:
  - `Generic Stall`
  - `Repeated-Starter Attractor`

5. behavior-first manipulation check
- visible arm-separation on first-move framing only
- protected boundary:
  - manipulated form remains compressed
  - one clear collapse row remains visible

## 4. Manuscript Integration Rule
Add or preserve manuscript-facing prose that does two things:
- names the state/action vocabulary that the current surface instantiates
- names the hold/break motif that the current surface supports or bounds
- keeps the repaired first-step side-topic/quarantine family distinct from the
  later-horizon family that reads its continuation

Do not:
- invent new policy states
- invent new dynamics motifs
- let vocabulary integration widen the underlying evidence claim
- let the auxiliary repaired downstream read or the bounded behavior-first note
  silently overwrite the current one-claim-plus-boundaries ontology
