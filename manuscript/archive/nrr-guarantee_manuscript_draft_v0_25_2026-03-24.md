# NRR-Guarantee Draft Outline v0.25

This draft snapshot carries the current manuscript-completion pass that produced
manuscript `v25`. The current emphasis is to keep the Core-to-Guarantee goal
fixed, keep the `non-evaluative principle` read in its narrower closure
discipline role, and improve main-text legibility through manuscript-native
tables, manuscript-native figures, tighter paper-native prose, and uniform
`two-provider mixed result` wording rather than memo-like framing.

## Role

This draft exists to turn the current `Guarantee-centered` architecture into a
manuscript-writing queue, not to widen the claim surface.

## Current write order

1. Introduction
   - keep anti-replacement boundary explicit
   - state `Guarantee` as the downstream bounded-claim line
2. Program position and scope
   - fix ownership of `Policy`, `Dynamics`, and `Policy-Verification` as
     supporting layers
3. Supporting-layer inputs
   - import only fixed vocabulary and protocol constraints
4. Claim unit and boundary discipline
   - first explicit fill is now the early ambiguity-turn claim unit
   - second explicit fill is now the provider-sensitive downstream-acceptance mixed read
   - third explicit fill is now the repaired first-step side-topic / quarantine unit
   - keep the eight guarantee fields visible for each later added unit
5. Evidence import surface
   - first manuscript-facing evidence paragraph now comes from the frozen
     ambiguity-turn pilot
   - second manuscript-facing evidence paragraph now comes from the repaired
     downstream-acceptance readout
   - third manuscript-facing evidence paragraph now comes from the repaired
     first-step side-topic / quarantine pilot
   - add one bounded behavior-first manipulation-check paragraph, but do not
     treat it as a fourth filled claim unit
   - later-horizon material now enters only as failure-boundary reporting
   - policy/dynamics vocabulary is now tied directly to each supported surface
   - convert the remaining evidence map into manuscript-facing section text
6. Reproducibility and package boundary
   - document build/verify wrappers and package coverage
7. Discussion
   - keep the non-evaluative principle as bounded engineering framing
8. Conclusion
   - state what is closed and what remains conditional
   - keep the final read staged as `Phase 1 = non-support`, `Phase 2 = mixed`

## Section-specific drafting TODOs

### Introduction
- add final bounded problem statement
- keep `NRR is not anti-LLM` and `conditional utility` explicit
- situate `Guarantee` against external mixed-initiative / clarification / grounding prior work without collapsing into those literatures

### Program position and scope
- explain why `Guarantee` is the current main downstream completion target
- keep `Energy` closed and `Transfer` deferred

### Supporting-layer inputs
- quote only the minimal policy vocabulary
- clarify that downstream unresolved-relation preservation is not only
  recommendation avoidance but also bounded retention of still-live user-side
  constraint / satisfaction structure
- summarize dynamics as mechanism vocabulary only
- summarize policy-verification as matched-condition forward validation only
- keep supporting-layer dependencies visible in section structure and trace tables rather than treating local memos as bibliography entries

### Claim unit and boundary discipline
- keep the first explicit bounded claim on the early ambiguity-turn surface
- keep the second bounded downstream surface explicitly provider-sensitive and mixed
- keep the third explicit bounded claim template-bounded on repaired first-step side-topic only
- state unsupported extensions in the main text

### Evidence import surface
- convert current memos into section-local prose and tables
- keep provider slices explicit before any pooled summary
- keep the behavior-first rerun in `Results` only as bounded
  behavioral-separation evidence with an attached limitation sentence
- keep one source/claim trace table in the main text so the bounded surfaces are auditable without reopening memo-style prose

### Later-horizon boundary
- do not add a fourth supported claim unit
- keep later-horizon text as explicit boundary / degradation reporting
- keep `C_incident_response at turn9` as the cleanest shared failure surface

### Policy/dynamics integration
- name which policy state/action each supported surface instantiates
- name which dynamics motif each supported surface or boundary corresponds to
- do not let vocabulary integration widen the underlying evidence
- smooth the manuscript-facing prose so these links read as one argument rather than an inventory

### Reproducibility and package boundary
- describe the repository surface as a bounded manuscript repository surface
- keep fallback build language factual rather than project-management oriented
- avoid review-pack/self-evaluation phrasing in the paper body

### Discussion
- keep theorem-level language out
- permit bounded explanation of `premature interpretation fixation` as
  contextual collapse inside an already accepted branch
- do not promote hidden-needs inference into a core manuscript claim
- place full cross-paper reinterpretation here only if it remains locally useful
- keep the Phase 2 result explicitly two-provider mixed rather than letting the
  Gemini-side support dominate the framing

## Immediate repo tasks after this scaffold

1. prepare the `v25` manuscript package with the bounded context-retention clarification
2. prepare the current manuscript package for independent external review
3. keep `two-provider mixed result` wording aligned across text, tables, and captions
4. sync the updated manuscript line into the mirror/review surfaces
5. run external cross-check before calling the line closed
