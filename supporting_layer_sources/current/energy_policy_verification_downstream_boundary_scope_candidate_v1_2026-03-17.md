# Energy Policy-Verification Downstream Boundary Scope Candidate v1

## Status
- Date: 2026-03-17
- Type: pilot-scope candidate memo
- Scope: choose the next narrow `Policy-Verification` pilot scope after the completed early ambiguity-turn pilot readout
- Depends on:
  - `energy_policy_verification_priority_resolution_baseline_vs_energy_policy_comparison_memo_v1_2026-03-17.md`
  - `energy_policy_verification_pilot_readout_external_review_decision_memo_v1_2026-03-17.md`
  - `energy_priority_resolution_downstream_acceptance_two_provider_read_memo_v1_2026-03-17.md`
  - `energy_priority_resolution_side_topic_commitment_operator_baseline_vs_operator_comparison_memo_v1_2026-03-17.md`
  - `energy_policy_minimal_spec_candidate_v2_2026-03-17.md`
  - `energy_guarantee_claim_schema_candidate_v2_2026-03-17.md`
- Not a run authorization memo
- Not a manuscript claim memo

## 1. Purpose
This memo chooses the next narrow `Policy-Verification` pilot scope after the early ambiguity-turn pilot has passed dual external review.

It does not:
- reopen the completed first pilot
- merge multiple downstream families into one claim
- authorize a run

Its role is narrower:
- decide which downstream bounded claim is honest enough to test next
- keep the next pilot aligned with the frozen `Policy` and `Guarantee` schema

## 2. Assistant-Side Read
- judgment:
  - `adopt`
- reason:
  - the next honest mainline move is the shared two-provider downstream boundary on preacceptance burden, while explicit suspended-objective quarantine is not yet ready as the next mainline `Policy-Verification` claim because the strongest current evidence there is still local/operator-specific rather than the same mainline `baseline vs Energy policy` surface

## 3. What Is Already Closed
Keep closed and do not reopen:
1. first `Policy-Verification` pilot on early ambiguity-turn non-unilateral priority resolution
2. completed `Energy` downstream-acceptance line as an `Energy` branch result
3. completed local side-topic commitment / followthrough operator lines as local branch evidence

This memo uses those closed results only to choose the next pilot boundary.

## 4. Next Pilot Choice
Choose one next pilot family only:
- `priority-resolution downstream boundary / premature-commitment suppression`

Do not choose as the immediate next pilot:
- `suspended-objective quarantine` as a combined claim with downstream burden removal

Reason for not combining them now:
1. the guarantee schema permits one family-bounded claim at a time
2. the clean shared two-provider mainline read currently available is:
  - preacceptance burden removal under downstream acceptance
3. the strongest current quarantine evidence is still narrower:
  - local side-first / operator-conditioned lines
  - not yet the same broad mainline `baseline vs Energy policy` verification surface

## 5. Targeted Claim-Field Fill
The next pilot should target the following bounded claim fields.

### 5.1 `provider_scope`
- tested provider pair only:
  - `anthropic`
  - `gemini`

### 5.2 `family_scope`
- one family only:
  - `priority-resolution downstream acceptance boundary`

### 5.3 `horizon_scope`
- one checkpoint band only:
  - the first accepted-direction follow-up after explicit user acceptance

### 5.4 `observability_scope`
- one frozen metric family only:
  - `accepted_direction_followthrough_present`
  - `rejected_direction_intrusion_absent`
  - `preacceptance_rework_burden_absent`
  - `appropriate`

### 5.5 `comparison_scope`
- mainline comparison only:
  - baseline vs Energy policy under matched conditions

### 5.6 `supported_behavior`
- reducing preacceptance burden relative to baseline under fixed conditions

### 5.7 `explicit_failure_boundary`
- accepted-direction followthrough can remain provider-sensitive even when preacceptance burden is removed
- downstream generic acknowledgment remains a visible residual on Gemini

### 5.8 `unsupported_extension`
- clean first-step side-topic commitment while keeping the prior objective quarantined
- broader suspended-objective quarantine claims
- later-horizon followthrough
- provider universality

## 6. Why This Scope Is Next
This scope is the next honest pilot because:
1. it is already supported by a real shared two-provider directional read
2. it stays on the mainline `baseline vs Energy policy` comparison shape
3. it carries one bounded downstream claim shape already allowed by the frozen guarantee schema
4. it avoids silently importing narrower local operator evidence as if it were already mainline verification

Current evidence read:
- Anthropic:
  - baseline `appropriate = 0 / 24`
  - Energy policy `appropriate = 24 / 24`
  - preacceptance burden removal `0 / 24 -> 24 / 24`
- Gemini:
  - baseline `appropriate = 0 / 24`
  - Energy policy `appropriate = 10 / 24`
  - preacceptance burden removal `0 / 24 -> 24 / 24`

Shared carry-forward read:
- the robust shared downstream gain is preacceptance burden removal
- the full downstream handoff quality is not yet uniformly provider-balanced

## 7. Why Quarantine Is Deferred
`suspended-objective quarantine` remains important, but it should not be bundled into this next pilot.

Current reason for deferral:
1. the clean local evidence exists on:
  - stricter side-topic commitment / explicit operator lines
2. that evidence is informative for `Policy` and `Dynamics`
3. but it is not yet the same next mainline `Policy-Verification` surface as:
  - tested provider pair
  - baseline vs Energy policy
  - one downstream family only

If a later memo wants to verify quarantine prospectively, it should open a separate pilot scope rather than widening this one.

## 8. Frozen Pilot Boundary
If this scope is adopted, the next exact-design memo should freeze:
1. the exact downstream-acceptance input artifacts
2. the exact provider/model tuple per provider
3. the exact baseline and Energy-policy prompt surfaces
4. the downstream metric reporting rule with provider slices shown before pooled totals
5. the explicit boundary wording:
  - preacceptance burden reduction is in scope
  - accepted-direction workstart quality may still diverge by provider

## 9. What This Pilot Would Answer
This next pilot would answer:
- under the tested provider pair and frozen downstream-acceptance family, does the Energy policy reduce preacceptance burden relative to baseline under matched conditions?

## 10. What This Pilot Would Not Answer
This next pilot would not answer:
- whether suspended-objective quarantine is already verified as the next mainline claim
- whether side-topic commitment is provider-balanced
- whether later-horizon followthrough is stable
- whether overall downstream task quality is improved

## 11. Review Question
Is this next `Policy-Verification` scope narrow and honest enough that:
- the downstream mainline claim stays on preacceptance burden suppression only
- explicit quarantine is deferred rather than silently bundled in
- the completed first pilot remains closed
